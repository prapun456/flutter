import 'package:flutter/material.dart';

class UserReels extends StatelessWidget {
  const UserReels ({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('ผมใส่video ไม่เป็นครับ',
      style: TextStyle(fontSize:40),),
    );
  }
}